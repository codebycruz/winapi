local ffi = require("ffi")

---@param wgl winapi.wgl
return function(wgl)
	---@class winapi.wgl.Enums
	local enums = {}

	-- WGL doesn't have many constants to expose currently
	-- This file exists for consistency with the module structure

	return enums
end
